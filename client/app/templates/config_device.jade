.clearfix
    .mod
        strong #{device.login}
        span &nbsp;-&nbsp;
        span.state-label= t('synchronized')

    .buttons.right
        button.remove-device.btn= t('revoke device access')
