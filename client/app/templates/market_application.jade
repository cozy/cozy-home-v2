.app-img.pull-left
    img(src="#{app.icon}")

.app-install-button.pull-right
    button.app-install +
    .app-install-text= t('add application ?')
.app-text
    h3 #{app.name}
    span.comment= t(app.comment)
    p #{app.description}
