should = require('chai').Should()

helpers = require '../helpers'
icons = require "../#{helpers.prefix}server/lib/icon"

describe "Icon helpers", ->

    describe.skip 'getPath', ->
    describe.skip 'getIconInfos', ->
    describe.skip 'save', ->

