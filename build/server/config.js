// Generated by CoffeeScript 1.9.0
var americano, config, e, fs, publicPath;

americano = require('americano');

fs = require('fs');

publicPath = __dirname + '/../client/public';

try {
  fs.lstatSync(publicPath);
} catch (_error) {
  e = _error;
  publicPath = __dirname + '/../../client/public';
}

config = {
  common: [
    americano.bodyParser(), americano.methodOverride(), americano.errorHandler({
      dumpExceptions: true,
      showStack: true
    }), americano["static"](publicPath, {
      maxAge: 86400000
    })
  ],
  development: [americano.logger('dev')],
  production: [americano.logger('short')],
  plugins: ['americano-cozy']
};

module.exports = config;
